{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "global-assistant",
  "description": "Listen-only event access for the read-only floating assistant window.",
  "windows": ["global-assistant"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
