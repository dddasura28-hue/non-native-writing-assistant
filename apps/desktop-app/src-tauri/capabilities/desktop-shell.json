{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-shell",
  "description": "Loads the local desktop writing shell without native API permissions.",
  "windows": ["main"],
  "permissions": []
}
