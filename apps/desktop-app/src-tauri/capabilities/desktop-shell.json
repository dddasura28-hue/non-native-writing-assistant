{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-shell",
  "description": "Loads the desktop writing shell. Fixed-purpose Rust commands handle credentials, provider settings, and provider HTTP.",
  "windows": ["main"],
  "permissions": []
}
